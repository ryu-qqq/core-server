logging:
  level:
    root: INFO  # 기본 로그 레벨 설정

---

spring:
  config:
    activate:
      on-profile: local

logging:
  config: classpath:logback-local.xml  # 로컬 환경에서 사용

#---
#
#spring:
#  config:
#    activate:
#      on-profile: dev
#
#logging:
#  config: classpath:logback-dev.xml  # 개발 환경에서 사용
#
#---
#
#spring:
#  config:
#    activate:
#      on-profile: prod
#
#logging:
#  config: classpath:logback-prod.xml  # 프로덕션 환경에서 사용
